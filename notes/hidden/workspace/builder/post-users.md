# Users by ID

- Method: `POST`
- Endpoint: `/multipass/api/users`
- Observed response: `200 OK`

```json
["<user UUID>"]
```

Bulk-resolves user IDs referenced by pipeline attribution into usernames and Multipass profile attributes for display on the landing page.
