///! Route that appends to https://www.palantir.com/docs/
pub type Route = Vec<String>;

///! Route that appends to [product | platform | updates]
pub type Scope = Vec<String>;

// Impl both Froms using Domain's methods.
